{
  "name": "tos-e2e",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "test": "npx playwright test",
    "install-browsers": "npx playwright install --with-deps"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}
